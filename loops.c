#include <stdio.h>
int main(void)
{
    //while loops 
    int i = 0 ;
    int n =5;
    while (i < n){
        printf("I'm in the  loop \n");
        i++;
    }
    printf(" That was a while loop \n");
    // For loops , these loops are mainly used when the condition can go up the defined number of times that are with in the certain boundary
    // for example; for ( int i=o; i< 4; i++) This means that when the  integer i is less than 4, let the loop remain running

    for (int y = 0; y < n; y++) {
        printf("This is the for loop\n");
    }
    printf("That was a for loop");

    // Do while loops 
    do {
//code
    } while (/* condition */)
    
}
